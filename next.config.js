/**
 * @type {import('next').NextConfig}
 */
 const nextConfig = {
    /* config options here */
    theme: {
      container: {
        screens: {
          '2xl': '1344px',
        },
      },
    },
  
  }
  
  module.exports = nextConfig